# NPM Configuration for optimal package publishing

# Configure default registry
registry=https://registry.npmjs.org/

# Publish with tag for better discoverability
tag=latest

# Configure access for scoped package
access=public
